package imageExt

// Format represents the image format
type Format string

const (
	// PNG represents the PNG image format
	PNG Format = "png"
	// JPEG represents the JPEG image format
	JPEG Format = "jpeg"
	// GIF represents the GIF image format
	GIF Format = "gif"
	// BMP represents the BMP image format
	BMP Format = "bmp"
	// WEBP represents the WebP image format
	WEBP Format = "webp"
)
